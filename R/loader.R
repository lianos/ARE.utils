# I want to be able to use the myutils package w/o having to install it
# into a library. Perhaps I can just install a myutils loader that will
# source the appropriate files and export the functions into a namespace.
